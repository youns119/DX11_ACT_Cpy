#pragma once

enum class SHADER_PASS_DEFERRED
{
	DEBUG,
	DEFFERED,
	FOG,
	POSTPROCESS_FINAL,
	QUANTIZATION,
	EDGE_DETECTION,
	BLOOM,
	SAMPLING,
	BLOOM_CURVE,
	BLEND_EFFECT,
	POSTPROCESS_DISTORTION,
	BLUR,
	BLOOM_ACC,
	COLOR_CORRECTION,
	UIBLUR,
};

enum class SHADER_PASS_LIGHTING
{
	DIRECTIONAL,
	POINT,
	SPOT,
};

enum class SHADER_PASS_SSAO
{
	COMPUTE_AO,
	APPLY_AO,
};

enum class SHADER_PASS_BLUR
{
	GAUSSIAN9_X,
	GAUSSIAN9_Y,
	GAUSSIAN5x5,
	GAUSSIAN5_X,
	GAUSSIAN5_Y,
	RADIAL,
	BOX,
	BILATERAL,

};